[pytest]
testpaths = backend/tests
markers =
    unit: fast unit tests with module stubs (default tier)
    integration: live appliance HTTP tests (DEV ON, https://localhost:8443)
    contract: OpenAPI contract tests via Schemathesis (DEV ON)
addopts = -m "not integration and not contract"
