{
    "name": "Docker Hello World",
    "description": "Deploy a Docker Hello World task",
    "fields": {
        "allocations_alloc1_resources_cpu_cores": {
            "label": "CPU Cores",
            "type": "number",
            "default": 0.5,
            "min": 0.5,
            "max": 4,
            "step": 0.5,
            "required": true,
            "description": "Number of CPU cores to allocate",
            "category": "resources"
        },
        "allocations_alloc1_resources_ram_size": {
            "label": "RAM Size (GiB)",
            "type": "number",
            "default": 0.5,
            "min": 0.5,
            "max": 8,
            "step": 0.5,
            "required": true,
            "description": "Amount of RAM to allocate in GiB",
            "category": "resources"
        },
        "allocations_alloc1_resources_disk_size": {
            "label": "Disk Size (GiB)",
            "type": "number",
            "default": 1,
            "min": 0.5,
            "max": 20,
            "required": true,
            "description": "Amount of disk space to allocate in GiB",
            "category": "resources"
        }
    }
} 
