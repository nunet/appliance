apiVersion: 1

providers:
  - name: "NuNet Telemetry"
    orgId: 1
    folder: "NuNet Telemetry"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
